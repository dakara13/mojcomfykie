services:
  comfyui:
    build: .
    container_name: comfyui
    restart: unless-stopped
    ports:
      - "8188:8188"
    volumes:
      - ./input:/app/input
      - ./output:/app/output
      - ./user:/app/user
